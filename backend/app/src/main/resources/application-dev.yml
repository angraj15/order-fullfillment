spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true

order:
  timer-duration: ${TIMER_DURATION:PT2M}

logging:
  level:
    com.example.orderfulfilment: DEBUG
    org.camunda: INFO
